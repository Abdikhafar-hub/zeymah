{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.5.2",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock;'",
    "dev": "npm --prefix artifacts/abayabuth-storefront run dev",
    "dev:api": "npm --prefix artifacts/api-server run dev",
    "build": "pnpm --filter @workspace/abayabuth-storefront run build && rm -rf dist && cp -r artifacts/abayabuth-storefront/dist ./dist",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
